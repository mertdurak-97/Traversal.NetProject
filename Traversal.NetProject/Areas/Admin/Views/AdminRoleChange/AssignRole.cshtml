@model List<RoleAssignViewModel>
@{
    ViewData["Title"] = "AssignRole";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h1>AssignRole</h1>
<form method="post">
    @for (var i = 0; i < Model.Count; i++)
    {
        <div class="form-check">
            <input type="hidden" asp-for="@Model[i].RoleID"/>
            <input type="hidden" asp-for="@Model[i].Name"/>
            <input type="checkbox" asp-for="@Model[i].Exists"/>
            <label class="form-check-label" for="@Model[i].Exists">@Model[i].Name</label>
        </div>
        
    }
    <button class="btn btn-info">Değişiklikleri Kaydet</button>
    <br />
</form>
<br />
 <a href="/Admin/AdminRole/UserRolesList/" class="btn btn-outline-primary">Kullanıcı Listesine Dön</a>